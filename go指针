//Go 拥有指针，指针保存了值的内存地址，
var a,b=1,2
var c *int
//将a的内存地址保存在c中(c的地址与a的内存地址不同，c只是保存a的内存地址)
c = &a
//读取c内存地址对应的值
fmt.Println(*c)
//将c的内存地址变为b的内存地址
c = &b
//取到的值为b的值
fmt.Prinln(*c)

//与python不一样 python创建的变量，只是引用，真正的内存地址是其引用的对象的内存地址
//所以通过变量间赋值的方式，go的变量的内存地址不变，而python的变量的内存地址会发生改变（改变后的引用指向的对象的内存地址）
